# Environment variables for MailVoyage Backend

# Port for the backend server
PORT=3001

# Allowed frontend host address (for security middleware)
# Example: HOST_ADDRESS=mailvoyage.in
HOST_ADDRESS=localhost:5173 # Default for Vite dev server

# Database connection string (Example)
# DATABASE_URL="your_database_connection_string"

# JWT Secret Key (USE A STRONG, RANDOM KEY IN PRODUCTION)
JWT_SECRET="YOUR_VERY_SECRET_KEY_CHANGE_ME"
JWT_EXPIRES_IN="1h" # Token expiry time
