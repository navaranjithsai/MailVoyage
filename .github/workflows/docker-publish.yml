# ──────────────────────────────────────────────────────────────
# MailVoyage — Auto-Versioned Build & Push to Docker Hub
#
# Versioning: CalVer — YYYY.M.BUILD (e.g. 2026.2.1, 2026.2.2)
#   • Build number auto-increments per month from existing git tags
#   • Every push to main gets a unique version — zero manual work
#
# Triggers:
#   • Push to main branch (auto-version + build + push)
#   • Manual via workflow_dispatch (optional tag override)
#
# Required GitHub Secrets:
#   DOCKERHUB_USERNAME  — your Docker Hub username
#   DOCKERHUB_TOKEN     — Docker Hub access token (NOT password)
#
# Automatically provided (no setup needed):
#   GITHUB_TOKEN        — used to create git tags & releases
#
# Docker Hub repo: navaranjithsai/mailvoyage
# GitHub repo:     navaranjithsai/MailVoyage
# ──────────────────────────────────────────────────────────────

name: Build & Push Docker Image

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'LICENSE'
      - '.github/ISSUE_TEMPLATE/**'
  workflow_dispatch:
    inputs:
      tag_override:
        description: 'Custom version tag (e.g. 2026.2.99-beta). Leave empty for auto CalVer.'
        required: false
        type: string

# Cancel in-flight runs for the same branch
concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

env:
  DOCKER_IMAGE: navaranjithsai/mailvoyage

jobs:
  # ────────────────────────────────────────────────────────────
  # Job 1: Read version from package.json (set locally via
  #        `npm run version:bump` or `npm run release`)
  #
  # The old auto-CalVer calculation is commented out below.
  # If you ever need CI to calculate versions again, uncomment
  # the "Calculate CalVer version" block and comment out
  # "Read version from package.json".
  # ────────────────────────────────────────────────────────────
  version:
    name: Read Version
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.calver.outputs.version }}
      version_tag: ${{ steps.calver.outputs.version_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version from package.json
        id: calver
        run: |
          # If manual override provided, use it directly
          if [[ -n "${{ inputs.tag_override }}" ]]; then
            VERSION="${{ inputs.tag_override }}"
          else
            # Read version that was set locally by `npm run version:bump`
            VERSION=$(jq -r '.version' package.json)
          fi

          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "version_tag=v${VERSION}" >> "$GITHUB_OUTPUT"

          echo "## Version" >> "$GITHUB_STEP_SUMMARY"
          echo "**v${VERSION}** (from package.json)" >> "$GITHUB_STEP_SUMMARY"

      # ── Old auto-CalVer calculation (commented out) ─────────
      # Kept for reference — uncomment if you want CI to
      # calculate versions instead of reading from package.json.
      #
      # - name: Calculate CalVer version
      #   id: calver
      #   run: |
      #     YEAR=$(date -u +%Y)
      #     MONTH=$(date -u +%-m)  # No zero-padding: 1-12
      #     PREFIX="v${YEAR}.${MONTH}"
      #
      #     if [[ -n "${{ inputs.tag_override }}" ]]; then
      #       VERSION="${{ inputs.tag_override }}"
      #       echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
      #       echo "version_tag=v${VERSION}" >> "$GITHUB_OUTPUT"
      #       exit 0
      #     fi
      #
      #     LATEST_BUILD=$(git tag --list "${PREFIX}.*" --sort=-version:refname | head -n1 | grep -oP '\d+$' || echo "0")
      #     NEXT_BUILD=$((LATEST_BUILD + 1))
      #     VERSION="${YEAR}.${MONTH}.${NEXT_BUILD}"
      #
      #     echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
      #     echo "version_tag=v${VERSION}" >> "$GITHUB_OUTPUT"

  build-and-push:
    name: Build & Push
    needs: version
    runs-on: ubuntu-latest
    permissions:
      contents: write   # Create tags + releases
      packages: write

    steps:
      # ── Checkout ──────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      # ── Old CI version sync (commented out) ──────────────────
      # Version is now bumped locally via `npm run version:bump`
      # before pushing. No CI commit needed = fewer commits.
      #
      # - name: Sync version in package.json files
      #   run: |
      #     VERSION="${{ needs.version.outputs.version }}"
      #     jq --arg v "$VERSION" '.version = $v' package.json > tmp.json && mv tmp.json package.json
      #     jq --arg v "$VERSION" '.version = $v' api/package.json > tmp.json && mv tmp.json api/package.json

      # ── Create git tag ────────────────────────────────────────
      # No version-bump commit needed — version was already
      # committed locally. CI only creates the tag.
      - name: Create version tag
        run: |
          VERSION_TAG="${{ needs.version.outputs.version_tag }}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Check if this tag already exists (locally or on remote)
          if git rev-parse "${VERSION_TAG}" >/dev/null 2>&1; then
            echo "⚠️ Tag ${VERSION_TAG} already exists — skipping tag creation"
            echo "### ⚠️ Tag ${VERSION_TAG} already exists (reusing)" >> "$GITHUB_STEP_SUMMARY"
          else
            git tag -a "${VERSION_TAG}" -m "Release ${VERSION_TAG}"
            git push origin "${VERSION_TAG}"
            echo "### ✅ Git tag created: ${VERSION_TAG}" >> "$GITHUB_STEP_SUMMARY"
          fi

      # ── Docker metadata (tags + labels) ───────────────────────
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ needs.version.outputs.version }}
            type=sha,prefix=sha-
          labels: |
            org.opencontainers.image.title=MailVoyage
            org.opencontainers.image.description=Modern, privacy-focused open-source email client
            org.opencontainers.image.version=${{ needs.version.outputs.version }}
            org.opencontainers.image.source=https://github.com/navaranjithsai/MailVoyage
            org.opencontainers.image.licenses=AGPL-3.0
            org.opencontainers.image.vendor=navaranjithsai

      # ── Set up Docker Buildx (multi-platform support) ─────────
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ── Login to Docker Hub ───────────────────────────────────
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ── Build and push ────────────────────────────────────────
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      # ── GitHub Release ────────────────────────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.version.outputs.version_tag }}
          name: MailVoyage ${{ needs.version.outputs.version_tag }}
          generate_release_notes: true
          body: |
            ## Docker Image

            ```bash
            docker pull navaranjithsai/mailvoyage:${{ needs.version.outputs.version }}
            ```

            **Tags published:**
            - `navaranjithsai/mailvoyage:${{ needs.version.outputs.version }}`
            - `navaranjithsai/mailvoyage:latest`
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ── Summary ───────────────────────────────────────────────
      - name: Build summary
        run: |
          echo "## 🐳 Docker Image Published" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** \`${{ needs.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Docker tags:**" >> $GITHUB_STEP_SUMMARY
          echo "- \`navaranjithsai/mailvoyage:${{ needs.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`navaranjithsai/mailvoyage:latest\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Pull:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker pull navaranjithsai/mailvoyage:${{ needs.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
